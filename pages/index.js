import Layout from '../components/layout'
import Uploader from '../components/uploader'

export default () => (
  <Layout>
    <div className="hero mt-2">
      <h4 className="text-center">Welcome to Now IPFS!</h4>
      <Uploader />
    </div>
  </Layout>
)
